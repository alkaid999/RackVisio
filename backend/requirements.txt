# RackVisio 机柜 3D 可视化平台后端依赖清单 (Phase 1)
# 开发/测试默认使用 SQLite；生产改 DATABASE_URL 为 postgresql+asyncpg 即切 PG，无需改代码。
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19            # 开发/测试 SQLite 异步驱动
asyncpg>=0.29              # 生产 PG 异步驱动（按 DATABASE_URL 加载，默认不依赖）
pydantic>=2.6
pydantic-settings>=2.2     # 环境变量配置
python-multipart>=0.0.9    # 表单/文件支持（预留）
pyyaml>=6.0                # 可选配置
redis>=5.0                 # 可选，REDIS_ENABLED=true 时启用（默认 false，内存字典替代）
# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                # 异步测试客户端
